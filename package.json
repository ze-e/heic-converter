{
  "name": "heic-converter-root",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install && rm -rf public && cp -r ../frontend/dist public",
    "start": "cd backend && npm start"
  }
}
